# Fan chat + paywall

Expo, React Native, TypeScript. Mock server deduplicates retried sends via
a stable client ID; state persists across offline periods and app
restarts. Paywall simulates purchase plus delayed backend confirmation.

Run: `npm install && npm start`
Tests: `npm test` (15 passing)

See [AI.md](AI.md) for AI usage.
